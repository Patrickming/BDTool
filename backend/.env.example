# 应用配置
NODE_ENV=development
PORT=3000

# 数据库配置
# 开发环境使用 SQLite
DATABASE_URL="file:./dev.db"

# 生产环境使用 PostgreSQL
# DATABASE_URL="postgresql://user:password@localhost:5432/kol_bd_tool"

# JWT 密钥配置
JWT_SECRET=your-super-secret-jwt-key-at-least-32-characters-long-change-this
JWT_EXPIRES_IN=7d

# CORS 配置（多个源用逗号分隔）
CORS_ORIGIN=http://localhost:5173,http://localhost:5174

# 翻译服务配置 (DeepL API)
# 申请地址: https://www.deepl.com/pro-api
# 免费额度: 每月 50 万字符
# 获取密钥: DeepL Account -> API Keys
# 注意: Free 版本和 Pro 版本使用不同的 API 密钥格式
#   - Free 版本密钥以 :fx 结尾
#   - Pro 版本密钥不带后缀
DEEPL_API_KEY=your-deepl-api-key-here:fx

# 日志级别
LOG_LEVEL=info

# 限流配置
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# AI 集成配置 (智谱AI GLM-4)
# 申请地址: https://open.bigmodel.cn/
# 获取密钥: 控制台 -> API Keys
GLM_API_KEY=your-glm-api-key-here
# 模型选择: glm-4-flash (免费/慢), glm-4-air (¥1/M), glm-4.5-airx (¥1/M/快), glm-4-plus (¥5/M), glm-4.5, glm-4.5-x, glm-4.6
# 推荐: glm-4.5-airx (速度快，成本低)
GLM_MODEL=glm-4.5-airx
# API 端点
GLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
# 请求超时（毫秒）- AI改写需要较长时间，建议60秒以上
GLM_TIMEOUT=60000
